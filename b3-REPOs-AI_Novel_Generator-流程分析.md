```
graph LR
    A[核心种子设定] --> B(角色动力学设定)
    B --> C{是否需要生成初始角色状态表};
    C -- 是 --> D[生成初始角色状态表];
    C -- 否 --> E(世界观构建);
    D --> E(世界观构建);
    E --> F(情节架构);
    F --> G(章节目录生成);
    G --> H{是否需要分块生成章节目录};
    H -- 是 --> I[分块生成章节目录];
    H -- 否 --> J[一次性生成章节目录];
    I --> K(章节草稿生成);
    J --> K(章节草稿生成);
    K --> L{是否需要知识库导入};
    L -- 是 --> M[知识库导入];
    L -- 否 --> N(章节定稿);
    M --> N(章节定稿);
    N --> O{是否需要扩写章节};
    O -- 是 --> P[扩写章节];
    O -- 否 --> Q(更新前文摘要);
    P --> Q(更新前文摘要);
    Q --> R(更新角色状态);
    R --> S(插入向量库);
    S --> T[完成];
```